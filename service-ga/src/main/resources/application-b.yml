spring:
  application.name: service-ga
  config.activate.on-profile: ga

ga:
  local:
    path: "/tmp/biblio/sedeB"    # almacenamiento simple E1
  server:
    rep:
      port: 9120                 # REP para Actores→GA

management:
  endpoints.web.exposure.include: health,info,prometheus
